diff --git a/apps/admin/lib/idempotency.ts b/apps/admin/lib/idempotency.ts
index 61b6bcc..d9ef898 100644
--- a/apps/admin/lib/idempotency.ts
+++ b/apps/admin/lib/idempotency.ts
@@ -17,6 +17,31 @@ export const ADMIN_COMMAND_ERROR_CODES = [
   "SETTLEMENT_RECOVERY_EXCEEDS_REMAINING", "SETTLEMENT_RECOVERY_NOT_PAID",
   "SETTLEMENT_TRANSITION_FORBIDDEN", "VALIDATION_ERROR", "VENUE_ANNOUNCEMENT_TOO_LATE",
   "SALES_GATE_REVISION_CONFLICT",
+  // PR-C: agent-referrals commands now go through the sanctioned mutation
+  // layer, so the codes their routes return are admin-command codes and
+  // must be classified here rather than inheriting the unknown-code default
+  // by accident. Three classes, and the difference matters:
+  //   * business refusals - the command never happened, a new key is safe;
+  //   * authority/state conflicts - the operator's view is stale, refresh;
+  //   * corruption (500-class) - the outcome is UNKNOWN, never mint a new
+  //     key over it, and every one of them also refreshes by status alone.
+  "AGENT_REFERRALS_CONTRACTOR_TYPE_PROJECTION_LOCKED", "AGENT_REFERRALS_LEGAL_IDENTITY_PROJECTION_LOCKED",
+  "AGENT_REFERRALS_CONTRACTOR_TYPE_PROJECTION_DIVERGED", "AGENT_REFERRALS_LEGAL_PROFILE_POINTER_DIVERGED",
+  "AGENT_REFERRALS_ACTIVATION_BINDING_CORRUPTED", "AGENT_REFERRALS_SETTLEMENT_LEGAL_PROFILE_BINDING_MISMATCH",
+  "AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_BLOCKED_BY_EXISTING_BINDING",
+  "AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_STALE",
+  "AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_INVALID_STATE",
+  "AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_NO_CHANGE",
+  "AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_ALREADY_PENDING",
+  "AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_INELIGIBLE_IDENTITY",
+  "AGENT_REFERRALS_LEGAL_PROFILE_REJECTED_COMBINATION", "AGENT_REFERRALS_LEGAL_PROFILE_EVIDENCE_REF_REQUIRED",
+  "AGENT_REFERRALS_LEGAL_PROFILE_REQUISITE_REQUIRED", "AGENT_REFERRALS_LEGAL_PROFILE_REQUISITE_FORBIDDEN",
+  "AGENT_REFERRALS_LEGAL_PROFILE_REQUISITE_INVALID_FORMAT",
+  "AGENT_REFERRALS_TAX_TREATMENT_MISSING", "AGENT_REFERRALS_TAX_TREATMENT_NPD_IS_SYSTEM_DERIVED",
+  "AGENT_REFERRALS_TAX_TREATMENT_MATRIX_REJECTED", "AGENT_REFERRALS_TAX_TREATMENT_RELATIONAL_INCONSISTENT",
+  "AGENT_REFERRALS_FEATURE_DORMANT", "AGENT_REFERRALS_SUSPENDED_BLOCKS_NEW_AUTHORITY",
+  "AGENT_REFERRALS_PARTNER_ALREADY_PROVISIONED", "AGENT_REFERRALS_PARTNER_PROMO_ALREADY_EXISTS",
+  "AGENT_REFERRALS_LEGAL_HOLD_ALREADY_ACTIVE",
 ] as const;
 
 export type AdminCommandErrorCode = (typeof ADMIN_COMMAND_ERROR_CODES)[number];
@@ -46,7 +71,24 @@ export const IDEMPOTENCY_DISPOSITION: Record<AdminCommandErrorCode, IdempotencyD
   RESERVATION_NOT_ABANDONABLE: "MAY_MINT_NEW_KEY", SETTLEMENT_BUSY: "MAY_MINT_NEW_KEY", SETTLEMENT_EXCEEDS_AVAILABLE: "MAY_MINT_NEW_KEY",
   SETTLEMENT_NOT_FOUND: "MAY_MINT_NEW_KEY", SETTLEMENT_RECOVERY_AMOUNT_INVALID: "MAY_MINT_NEW_KEY",
   SETTLEMENT_RECOVERY_EXCEEDS_REMAINING: "MAY_MINT_NEW_KEY", SETTLEMENT_RECOVERY_NOT_PAID: "MAY_MINT_NEW_KEY",
-  SETTLEMENT_TRANSITION_FORBIDDEN: "MAY_MINT_NEW_KEY", VALIDATION_ERROR: "MAY_MINT_NEW_KEY", VENUE_ANNOUNCEMENT_TOO_LATE: "MAY_MINT_NEW_KEY",
+  SETTLEMENT_TRANSITION_FORBIDDEN: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_CONTRACTOR_TYPE_PROJECTION_LOCKED: "MAY_MINT_NEW_KEY", AGENT_REFERRALS_LEGAL_IDENTITY_PROJECTION_LOCKED: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_CONTRACTOR_TYPE_PROJECTION_DIVERGED: "RETAIN_KEY", AGENT_REFERRALS_LEGAL_PROFILE_POINTER_DIVERGED: "RETAIN_KEY",
+  AGENT_REFERRALS_ACTIVATION_BINDING_CORRUPTED: "RETAIN_KEY", AGENT_REFERRALS_SETTLEMENT_LEGAL_PROFILE_BINDING_MISMATCH: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_BLOCKED_BY_EXISTING_BINDING: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_STALE: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_INVALID_STATE: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_NO_CHANGE: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_ALREADY_PENDING: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_LEGAL_PROFILE_SUPERSESSION_INELIGIBLE_IDENTITY: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_LEGAL_PROFILE_REJECTED_COMBINATION: "MAY_MINT_NEW_KEY", AGENT_REFERRALS_LEGAL_PROFILE_EVIDENCE_REF_REQUIRED: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_LEGAL_PROFILE_REQUISITE_REQUIRED: "MAY_MINT_NEW_KEY", AGENT_REFERRALS_LEGAL_PROFILE_REQUISITE_FORBIDDEN: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_LEGAL_PROFILE_REQUISITE_INVALID_FORMAT: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_TAX_TREATMENT_MISSING: "MAY_MINT_NEW_KEY", AGENT_REFERRALS_TAX_TREATMENT_NPD_IS_SYSTEM_DERIVED: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_TAX_TREATMENT_MATRIX_REJECTED: "MAY_MINT_NEW_KEY", AGENT_REFERRALS_TAX_TREATMENT_RELATIONAL_INCONSISTENT: "RETAIN_KEY",
+  AGENT_REFERRALS_FEATURE_DORMANT: "MAY_MINT_NEW_KEY", AGENT_REFERRALS_SUSPENDED_BLOCKS_NEW_AUTHORITY: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_PARTNER_ALREADY_PROVISIONED: "MAY_MINT_NEW_KEY", AGENT_REFERRALS_PARTNER_PROMO_ALREADY_EXISTS: "MAY_MINT_NEW_KEY",
+  AGENT_REFERRALS_LEGAL_HOLD_ALREADY_ACTIVE: "MAY_MINT_NEW_KEY", VALIDATION_ERROR: "MAY_MINT_NEW_KEY", VENUE_ANNOUNCEMENT_TOO_LATE: "MAY_MINT_NEW_KEY",
   SALES_GATE_REVISION_CONFLICT: "MAY_MINT_NEW_KEY",
 };
 
@@ -64,5 +106,35 @@ export function safeToMintNewKey(error: AdminApiError): boolean {
   return IDEMPOTENCY_DISPOSITION[error.code as AdminCommandErrorCode] === "MAY_MINT_NEW_KEY";
 }
 
-export const shouldRefreshAuthoritativeState = (error: AdminApiError) =>
+/**
+ * Structural, not class-bound: AdminApiError and PartnerApiError carry the
+ * same two fields, and "an ambiguous or state-changing failure must refresh
+ * authoritative state" is one policy, not two. Taking the shape rather than
+ * the class lets both realms share it without either importing the other's
+ * error type (PR-C).
+ */
+/**
+ * PR-C2 review round 2, P1: the client-side half of every `STALE_BOUND`
+ * proof.
+ *
+ * The server-side proof holds only if the RETRY carries the same observed
+ * version the original attempt did. But this layer deliberately refreshes
+ * authoritative state on an ambiguous outcome, and a form that re-derives
+ * its `expected_*` pin from the refreshed query afterwards is no longer
+ * retrying A - it is authoring a NEW command against B's state, with A's
+ * body. The server would apply it, correctly, because that is what the
+ * request now says.
+ *
+ * So the command's own variables - pin included - must survive the
+ * ambiguity, exactly as the idempotency KEY does. The rule is the same one,
+ * and is read from the same table rather than being a second policy that
+ * can drift: if the outcome leaves it unsafe to mint a new key, it is also
+ * unsafe to re-derive the pin. A definitive business refusal means the
+ * command did not happen, so a fresh pin is right and the intent is
+ * discarded; anything ambiguous, unknown, or 500-class is retained.
+ */
+export const shouldRetainCommandIntent = (error: { status: number; code: string }) =>
+  !safeToMintNewKey(error as AdminApiError);
+
+export const shouldRefreshAuthoritativeState = (error: { status: number; code: string }) =>
   error.status === 0 || error.status >= 500 || REFRESH_DISPOSITION[error.code as AdminCommandErrorCode] !== "NO_REFRESH_NEEDED";
