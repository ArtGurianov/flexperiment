name: Test

on:
  pull_request:
  push:
  workflow_dispatch: {}

concurrency:
  group: flexperiment-test-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          # The durable release refs are not part of a default checkout, so
          # without this the ancestry fence silently skips in the one place it
          # is meant to run.
          fetch-depth: 0
      - name: Fetch durable release refs the topology fence judges
        run: |
          set -euo pipefail
          git fetch --no-tags origin \
            '+refs/heads/production-deploy:refs/remotes/origin/production-deploy' \
            '+refs/heads/main:refs/remotes/origin/main' || true
      - uses: pnpm/action-setup@v4
        with:
          version: 10.29.3
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      # next-env.d.ts imports ./.next/types/*.d.ts, which only exist after a
      # build or typegen. Without them that file fails to load, taking its
      # next/image-types/global reference down with it, so a clean checkout
      # reports PageProps/LayoutProps and every image module as missing.
      # Locally these are masked by leftover .next/dev/types from next dev.
      - run: pnpm exec next typegen
      - run: pnpm exec tsc --noEmit
      - run: pnpm test
      - name: Real-Nginx frontend static-export routing contract
        run: pnpm frontend:test:nginx
      - name: Real-Nginx admin dynamic-upstream config
        run: pnpm admin:test:nginx
